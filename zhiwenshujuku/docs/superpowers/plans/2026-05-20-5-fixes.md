# 5项改进实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复数据字典展示、查询历史批量操作/日期筛选/渲染性能、侧边栏卡片统一、系统概览缓存

**Architecture:** 5 个独立任务，每个任务只修改相关文件，互不依赖。后端新增 `/data-dictionary` 和 `/history/batch-delete` 端点，前端用缓存函数 + 组件统一 + 渲染优化解决性能问题。

**Tech Stack:** FastAPI, Streamlit, Plotly, SQLite

---

### Task 1: 创建数据字典文件 + 后端端点 + 前端展示

**Files:**
- Create: `knowledge/data_dictionary.yml`
- Modify: `backend/api.py` — 在 `/schema/relations` 端点后追加新端点
- Modify: `frontend/utils.py` — 添加 `get_data_dictionary()` 函数
- Modify: `frontend/views/系统概览.py` — 新增 "📖 数据字典" tab

- [ ] **Step 1: 创建数据字典文件**

创建 `knowledge/data_dictionary.yml`，根据 movies 表结构编写字段说明：

```yaml
movies:
  name: 电影信息表
  description: 存储电影基本信息，包含标题、年份、导演、评分等核心字段
  columns:
    - field: id
      type: INTEGER
      key: PK
      description: 电影唯一编号
    - field: title
      type: TEXT
      description: 电影中文标题
    - field: original_title
      type: TEXT
      description: 电影原始语言标题
    - field: year
      type: INTEGER
      description: 上映年份，部分早期电影为 0
    - field: director
      type: TEXT
      description: 导演姓名，部分包含多个导演
    - field: actors
      type: TEXT
      description: 主要演员，逗号分隔
    - field: genre
      type: TEXT
      description: 电影类型/风格，逗号分隔多个标签
    - field: country
      type: TEXT
      description: 制片国家/地区
    - field: language
      type: TEXT
      description: 影片主要语言
    - field: duration
      type: INTEGER
      description: 影片时长（分钟），0 表示未知
    - field: rating
      type: REAL
      description: 豆瓣评分（0-10），0 表示暂无评分
    - field: rating_count
      type: INTEGER
      description: 评分人数
    - field: description
      type: TEXT
      description: 电影简介/剧情梗概

users:
  name: 用户信息表
  description: 存储平台注册用户的基本信息
  columns:
    - field: id
      type: INTEGER
      key: PK
      description: 用户唯一编号
    - field: username
      type: TEXT
      description: 用户昵称
    - field: city
      type: TEXT
      description: 用户所在城市
    - field: age
      type: INTEGER
      description: 用户年龄
    - field: gender
      type: TEXT
      description: 用户性别
    - field: registration_date
      type: TEXT
      description: 注册日期 (YYYY-MM-DD)

reviews:
  name: 电影评论表
  description: 存储用户对电影的评分和评论内容
  columns:
    - field: id
      type: INTEGER
      key: PK
      description: 评论唯一编号
    - field: movie_id
      type: INTEGER
      key: FK
      description: 关联的电影编号 → movies.id
    - field: user_id
      type: INTEGER
      key: FK
      description: 关联的用户编号 → users.id
    - field: rating
      type: REAL
      description: 用户评分（0-10）
    - field: review_text
      type: TEXT
      description: 评论文本内容
    - field: review_date
      type: TEXT
      description: 评论日期 (YYYY-MM-DD)

query_history:
  name: 查询历史表
  description: 记录用户通过自然语言或 SQL 编辑器发起的查询
  columns:
    - field: id
      type: INTEGER
      key: PK
      description: 记录唯一编号
    - field: session_id
      type: TEXT
      description: 会话编号，关联一次对话
    - field: question
      type: TEXT
      description: 用户提问的自然语言或 "[直接SQL]"
    - field: sql_generated
      type: TEXT
      description: 系统生成的 SQL 语句
    - field: sql_executed
      type: TEXT
      description: 实际执行的 SQL 语句
    - field: execution_status
      type: TEXT
      description: 执行状态：success / failure
    - field: result_summary
      type: TEXT
      description: 结果摘要，如 "42 rows"
    - field: is_favorite
      type: INTEGER
      description: 是否收藏：0 否 / 1 是
    - field: created_at
      type: TEXT
      description: 记录创建时间
```

- [ ] **Step 2: 后端新增 `/data-dictionary` 端点**

修改 `backend/api.py`，在 `/schema/relations` 端点后添加：

```python
@app.get("/data-dictionary")
async def get_data_dictionary():
    """获取数据字典（字段中文说明）"""
    import yaml
    from backend.config import DATA_DICTIONARY_PATH
    try:
        with open(DATA_DICTIONARY_PATH, encoding="utf-8") as f:
            return yaml.safe_load(f)
    except FileNotFoundError:
        return {}
```

- [ ] **Step 3: 前端 utils.py 添加获取函数**

修改 `frontend/utils.py`，在 `get_health()` 后添加：

```python
def get_data_dictionary() -> dict | None:
    """获取数据字典（缓存 5 分钟）"""
    return _cached_get("/data-dictionary")
```

- [ ] **Step 4: 系统概览页添加数据字典 tab**

修改 `frontend/views/系统概览.py` 的 `page()` 函数，将 `tab1, tab2` 改为 `tab1, tab2, tab3`，并在文件末尾添加 `_render_data_dictionary()` 函数。

修改 `page()` 中 tabs 相关行（第 56-62 行）：

原代码：
```python
    tab1, tab2 = st.tabs(["📊 查询统计", "🗃️ 数据库 ER 图"])

    with tab1:
        _render_query_stats()

    with tab2:
        _render_er_diagram()
```

替换为：
```python
    tab1, tab2, tab3 = st.tabs(["📊 查询统计", "🗃️ 数据库 ER 图", "📖 数据字典"])

    with tab1:
        _render_query_stats()

    with tab2:
        _render_er_diagram()

    with tab3:
        _render_data_dictionary()
```

在文件末尾（`_box_half_size` 函数之后）添加数据字典渲染函数和导入：

修改文件顶部 import，添加：
```python
from frontend.utils import api_get, get_insights, get_data_dictionary
```

在 `_box_half_size` 函数后添加：

```python
def _render_data_dictionary():
    """渲染数据字典 tab"""
    dd = get_data_dictionary()
    if not dd:
        st.warning("无法加载数据字典。")
        return

    for table_name, table_info in dd.items():
        if not isinstance(table_info, dict):
            continue
        color = TABLE_COLORS.get(table_name, "#666")
        with st.expander(f"📄 {table_name} — {table_info.get('name', '')}", expanded=False):
            st.markdown(f"_{table_info.get('description', '')}_")
            rows = []
            for col in table_info.get("columns", []):
                key_tag = ""
                if col.get("key") == "PK":
                    key_tag = "🔑 主键"
                elif col.get("key") == "FK":
                    key_tag = "🔗 外键"
                rows.append({
                    "字段": col.get("field", ""),
                    "类型": col.get("type", ""),
                    "说明": col.get("description", ""),
                    "约束": key_tag,
                })
            st.dataframe(pd.DataFrame(rows), hide_index=True, width="stretch")
```

- [ ] **Step 5: 验证**

运行: `python -c "import yaml; print(yaml.safe_load(open('D:/cursor_workplace/zhiwenshujuku/knowledge/data_dictionary.yml', encoding='utf-8')))"`
预期: 输出完整字典数据，无异常

- [ ] **Step 6: Commit**

```bash
git add knowledge/data_dictionary.yml backend/api.py frontend/utils.py frontend/views/系统概览.py
git commit -m "feat: 添加数据字典 YAML 文件、后端端点及前端展示 tab"
```

---

### Task 2: 查询历史页面重写 — 日期筛选 + 批量删除 + 渲染优化

**Files:**
- Modify: `backend/database.py` — `get_history()` 支持日期范围参数，新增 `batch_delete_history()`
- Modify: `backend/api.py` — 更新 `/history` 端点支持日期参数，新增 `/history/batch-delete` 端点
- Modify: `frontend/utils.py` — 新增 `batch_delete_history()`
- Modify: `frontend/views/查询历史.py` — 完整重写

- [ ] **Step 1: 后端 database.py 更新 get_history 支持日期范围**

修改 `backend/database.py` 的 `get_history` 方法（第 135-153 行）：

```python
def get_history(self, session_id: str | None = None, limit: int = 200,
                date_from: str | None = None, date_to: str | None = None) -> list[dict]:
    """获取查询历史，支持日期范围筛选"""
    conn = self.get_connection()
    cursor = conn.cursor()

    conditions = []
    params = []

    if session_id:
        conditions.append("session_id = ?")
        params.append(session_id)
    if date_from:
        conditions.append("DATE(created_at) >= ?")
        params.append(date_from)
    if date_to:
        conditions.append("DATE(created_at) <= ?")
        params.append(date_to)

    where_clause = f"WHERE {' AND '.join(conditions)}" if conditions else ""
    sql = f"SELECT * FROM query_history {where_clause} ORDER BY created_at DESC LIMIT ?"
    params.append(limit)

    cursor.execute(sql, tuple(params))
    rows = [dict(row) for row in cursor.fetchall()]
    conn.close()
    return rows
```

- [ ] **Step 2: 后端 database.py 新增 batch_delete_history**

在 `get_history` 方法后添加：

```python
def batch_delete_history(self, ids: list[int]) -> int:
    """批量删除查询历史，返回删除行数"""
    if not ids:
        return 0
    conn = self.get_connection()
    placeholders = ",".join(["?"] * len(ids))
    cursor = conn.execute(
        f"DELETE FROM query_history WHERE id IN ({placeholders})",
        tuple(ids)
    )
    conn.commit()
    deleted = cursor.rowcount
    conn.close()
    return deleted
```

- [ ] **Step 3: 后端 api.py 更新 /history 端点 + 新增批量删除**

修改 `/history` 端点（第 332-335 行），添加日期参数：

```python
@app.get("/history")
async def get_history(session_id: str | None = None, limit: int = 200,
                      date_from: str | None = None, date_to: str | None = None):
    """获取查询历史，支持日期范围筛选"""
    return app_state.db.get_history(session_id, limit, date_from, date_to)
```

在 `/history/{history_id}` DELETE 端点后添加批量删除端点：

```python
@app.post("/history/batch-delete")
async def batch_delete_history(data: dict):
    """批量删除查询历史"""
    ids = data.get("ids", [])
    if not ids:
        raise HTTPException(status_code=400, detail="ids 不能为空")
    deleted = app_state.db.batch_delete_history(ids)
    return {"success": True, "deleted": deleted}
```

- [ ] **Step 4: 前端 utils.py 添加 batch_delete_history**

在 `toggle_favorite` 函数后添加：

```python
def batch_delete_history(ids: list[int]) -> dict | None:
    """批量删除历史记录"""
    return api_post("/history/batch-delete", {"ids": ids})
```

- [ ] **Step 5: 重写查询历史页面**

完全重写 `frontend/views/查询历史.py`：

```python
"""
智问数据库 — 查询历史页面
"""

import streamlit as st
import pandas as pd
from datetime import date, timedelta
from frontend.utils import get_history, execute_sql, delete_history, toggle_favorite, batch_delete_history
from frontend.styles import page_header

PAGE_SIZE = 20


def page():
    st.markdown(
        page_header(
            "📜", "查询历史",
            "查看和管理所有查询记录",
            gradient="linear-gradient(135deg, #6A1B9A, #8E24AA)",
        ),
        unsafe_allow_html=True,
    )

    # ========== 筛选行 ==========
    f1, f2, f3, f4 = st.columns(4)
    with f1:
        search = st.text_input("🔍 搜索", placeholder="问题或 SQL 关键词...", label_visibility="collapsed")
    with f2:
        status_filter = st.selectbox("状态", ["全部", "success", "failure"], label_visibility="collapsed")
    with f3:
        date_from = st.date_input("开始日期", value=None, label_visibility="collapsed")
    with f4:
        date_to = st.date_input("结束日期", value=None, label_visibility="collapsed")

    # ========== 获取数据 ==========
    df = date_from.isoformat() if date_from else None
    dt = date_to.isoformat() if date_to else None
    history = get_history(limit=500, date_from=df, date_to=dt)

    if not history:
        st.info("📭 暂无查询历史。前往「对话查询」或「SQL 编辑器」页面进行查询吧！")
        return

    # 前端过滤（搜索、状态、收藏）
    if search:
        s = search.lower()
        history = [h for h in history if s in h.get("question", "").lower()
                   or s in h.get("sql_generated", "").lower()
                   or s in h.get("sql_executed", "").lower()]
    if status_filter != "全部":
        history = [h for h in history if h.get("execution_status") == status_filter]

    if not history:
        st.info("🔍 没有匹配的记录，试试调整筛选条件。")
        return

    # ========== 构建 DataFrame ==========
    rows = []
    for h in history:
        sql = (h.get("sql_generated") or h.get("sql_executed") or "")[:80]
        rows.append({
            "id": h.get("id"),
            "时间": h.get("created_at", "")[:16] if h.get("created_at") else "",
            "问题": h.get("question", "(直接SQL)")[:60],
            "SQL": sql + ("..." if len(h.get("sql_generated", "") or h.get("sql_executed", "") or "") > 80 else ""),
            "状态": "✅" if h.get("execution_status") == "success" else "❌",
            "结果": h.get("result_summary", ""),
            "收藏": "⭐" if h.get("is_favorite") else "☆",
        })

    df = pd.DataFrame(rows)

    # ========== 批量操作栏 ==========
    total = len(df)
    st.markdown(f"共 **{total}** 条记录")

    bc1, bc2, bc3, bc4 = st.columns([3, 1, 1, 1])
    with bc1:
        selected_ids = st.multiselect(
            "选择记录", df["id"].tolist(),
            format_func=lambda x: f"#{x}",
            label_visibility="collapsed",
            placeholder="选择要批量操作的历史记录...",
        )
    with bc2:
        if st.button("🗑️ 批量删除", disabled=not selected_ids, width="stretch"):
            result = batch_delete_history(selected_ids)
            if result and result.get("success"):
                st.success(f"已删除 {result.get('deleted', 0)} 条记录")
                st.rerun()
            else:
                st.error("删除失败")
    with bc3:
        show_favorites = st.checkbox("⭐ 仅收藏")
    if show_favorites:
        df = df[df["收藏"] == "⭐"]
        if df.empty:
            st.info("没有收藏的记录。")
            return

    # ========== 数据表格 ==========
    st.dataframe(
        df.drop(columns=["id"]),
        width="stretch",
        hide_index=True,
        height=520,
        column_config={
            "时间": st.column_config.TextColumn(width="small"),
            "问题": st.column_config.TextColumn(width="medium"),
            "SQL": st.column_config.TextColumn(width="medium"),
            "状态": st.column_config.TextColumn(width="small"),
            "结果": st.column_config.TextColumn(width="small"),
            "收藏": st.column_config.TextColumn(width="small"),
        },
    )

    # ========== 单条操作 ==========
    st.markdown("---")
    st.markdown("### 🔧 单条记录操作")

    selected_id = st.selectbox(
        "选择记录 ID 进行操作",
        [h["id"] for h in history],
        format_func=lambda x: f"#{x} — {next((h.get('question', '')[:50] for h in history if h.get('id') == x), '')}",
    )

    if selected_id:
        record = next((h for h in history if h.get("id") == selected_id), None)
        if record:
            c1, c2, c3 = st.columns(3)
            with c1:
                if st.button("🔄 重新执行", width="stretch"):
                    sql = record.get("sql_generated") or record.get("sql_executed", "")
                    with st.spinner("🔄 执行中..."):
                        result = execute_sql(sql)
                    if result and result.get("success") and result.get("data"):
                        st.success(f"✅ 返回 {len(result['data'])} 行")
                        st.dataframe(pd.DataFrame(result["data"]), hide_index=True)
                    else:
                        st.error(f"❌ {result.get('error', '执行失败') if result else '连接失败'}")
            with c2:
                fav_label = "⭐ 取消收藏" if record.get("is_favorite") else "☆ 收藏"
                if st.button(fav_label, width="stretch"):
                    toggle_favorite(selected_id)
                    st.rerun()
            with c3:
                if st.button("🗑️ 删除此记录", width="stretch"):
                    delete_history(selected_id)
                    st.rerun()

            # 显示完整 SQL
            sql_full = record.get("sql_generated") or record.get("sql_executed") or ""
            if sql_full:
                st.code(sql_full, language="sql")
```

- [ ] **Step 6: 验证后端批量删除**

启动后端后运行:
```bash
curl -X POST http://localhost:8765/history/batch-delete -H "Content-Type: application/json" -d "{\"ids\": [99999]}"
```
预期: `{"success":true,"deleted":0}`（不存在的 ID 返回 0）

- [ ] **Step 7: Commit**

```bash
git add backend/database.py backend/api.py frontend/utils.py frontend/views/查询历史.py
git commit -m "feat: 查询历史增加日期筛选、批量删除，重写为 dataframe 渲染提升性能"
```

---

### Task 3: 侧边栏指标卡片统一

**Files:**
- Modify: `frontend/app.py:67-80` — 替换 `st.metric()` 为 `metric_cards_html()`

- [ ] **Step 1: 修改 app.py 侧边栏指标区域**

将 `frontend/app.py` 第 72-80 行：

```python
    # 统计概览（带缓存）
    insights = get_insights()
    if insights:
        st.markdown("**数据概览**")
        cols = st.columns(2)
        cols[0].metric("🎬 电影", insights.get("total_movies", 0))
        cols[1].metric("⭐ 均分", insights.get("avg_rating", 0))
        cols[0].metric("👥 用户", insights.get("total_users", 0))
        cols[1].metric("📝 评论", insights.get("total_reviews", 0))
```

替换为：

```python
    # 统计概览（带缓存，使用统一卡片样式）
    insights = get_insights()
    if insights:
        st.markdown("**数据概览**")
        row1_cards = [
            ("🎬 电影总数", str(insights.get("total_movies", 0)), "#1E3A5F"),
            ("⭐ 平均评分", str(insights.get("avg_rating", 0)), "#E65100"),
        ]
        row2_cards = [
            ("👥 用户数", str(insights.get("total_users", 0)), "#2E7D32"),
            ("📝 评论数", str(insights.get("total_reviews", 0)), "#6A1B9A"),
        ]
        st.markdown(metric_cards_html(row1_cards), unsafe_allow_html=True)
        st.markdown(metric_cards_html(row2_cards), unsafe_allow_html=True)
```

同时在 app.py 顶部 import 中添加 `metric_cards_html`：

```python
from frontend.styles import GLOBAL_CSS, metric_cards_html
```

即修改第 6 行：
```python
from frontend.styles import GLOBAL_CSS
```
改为：
```python
from frontend.styles import GLOBAL_CSS, metric_cards_html
```

- [ ] **Step 2: 同时修复 app.py 中侧边栏 hr 颜色**

修改 `app.py` 第 62-64 行：

```python
    st.markdown(
        "<hr style='border-color: rgba(255,255,255,0.1);'>",
        unsafe_allow_html=True,
    )
```

改为（匹配浅蓝背景）：

```python
    st.markdown(
        "<hr style='border-color: rgba(30,58,95,0.12);'>",
        unsafe_allow_html=True,
    )
```

- [ ] **Step 3: Commit**

```bash
git add frontend/app.py
git commit -m "style: 侧边栏指标统一使用 metric_cards_html 卡片样式，修复分隔线颜色"
```

---

### Task 4: 系统概览页添加缓存

**Files:**
- Modify: `frontend/views/系统概览.py` — `_render_query_stats()` 和 `_render_er_diagram()` 内联缓存

- [ ] **Step 1: 缓存查询统计数据**

修改 `_render_query_stats()` 函数（第 65-98 行），将 `api_get("/stats/queries")` 替换为缓存版本。在函数开头添加：

`frontend/views/系统概览.py` 第 65-67 行，在函数定义后、`stats = api_get(...)` 之前插入：

```python
def _render_query_stats():
    @st.cache_data(ttl=120)
    def _cached_query_stats():
        return api_get("/stats/queries")

    stats = _cached_query_stats()
```

同时将原来第 66 行的 `stats = api_get("/stats/queries")` 删除。

实际修改：将第 65-67 行的：
```python
def _render_query_stats():
    stats = api_get("/stats/queries")
```
替换为：
```python
def _render_query_stats():
    @st.cache_data(ttl=120)
    def _cached_query_stats():
        return api_get("/stats/queries")

    stats = _cached_query_stats()
```

- [ ] **Step 2: 缓存 ER 图数据**

修改 `_render_er_diagram()` 函数（第 101-170 行），同样添加缓存。将第 101-102 行的：

```python
def _render_er_diagram():
    relations = api_get("/schema/relations")
```

替换为：

```python
def _render_er_diagram():
    @st.cache_data(ttl=300)
    def _cached_schema_relations():
        return api_get("/schema/relations")

    relations = _cached_schema_relations()
```

- [ ] **Step 3: Commit**

```bash
git add frontend/views/系统概览.py
git commit -m "perf: 系统概览查询统计和 ER 图数据添加 streamlit 缓存"
```

---

### Task 5: 更新 get_history 前端调用以支持日期参数

**Files:**
- Modify: `frontend/utils.py:70-73` — `get_history()` 增加日期参数

- [ ] **Step 1: 更新 get_history 函数签名**

修改 `frontend/utils.py` 第 70-73 行：

```python
def get_history(session_id: str | None = None, limit: int = 200,
                date_from: str | None = None, date_to: str | None = None) -> list:
    """获取查询历史，支持日期范围筛选"""
    params = []
    if session_id:
        params.append(f"session_id={session_id}")
    if date_from:
        params.append(f"date_from={date_from}")
    if date_to:
        params.append(f"date_to={date_to}")
    params.append(f"limit={limit}")
    query = "&".join(params)
    result = api_get(f"/history?{query}")
    return result if isinstance(result, list) else []
```

- [ ] **Step 2: Commit**

```bash
git add frontend/utils.py
git commit -m "feat: get_history 前端函数支持日期范围参数"
```

---

## 执行顺序

Task 1-5 互相独立，可并行执行。推荐顺序：

1. **Task 1** (数据字典) — 纯新增功能，无破坏性变更
2. **Task 3** (卡片统一) — 单文件小改动，快速见效
3. **Task 4** (缓存) — 单文件小改动
4. **Task 5** (日期参数) — 为 Task 2 做准备
5. **Task 2** (历史重写) — 改动最大，最后做

## 测试要点

- 数据字典 tab 在系统概览页能正常切换显示
- 查询历史日期筛选返回正确的日期范围结果
- 批量删除选中的记录，刷新后记录消失
- 侧边栏卡片与数据可视化页卡片视觉一致
- 系统概览 tab 切换不再触发网络请求（缓存命中）
